import type { AppThemeMode } from '../types/themes';

/** Modo por defecto del App Shell */
export const DEFAULT_APP_THEME: AppThemeMode = 'light';

/** Clave de localStorage para persistir el modo del App Shell */
export const APP_THEME_STORAGE_KEY = 'sif-app-theme';
